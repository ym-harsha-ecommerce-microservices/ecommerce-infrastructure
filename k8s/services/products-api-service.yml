apiVersion: v1
kind: Service
metadata:
  name: products-api-service
  namespace: ecommerce-infrastructure
  labels:
    app: products-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: products-api
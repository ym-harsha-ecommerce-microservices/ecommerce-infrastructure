apiVersion: v1
kind: Service
metadata:
  name: users-postgres-db-service
  namespace: ecommerce-infrastructure
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: users-postgres-db


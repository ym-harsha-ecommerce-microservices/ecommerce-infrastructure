apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: ecommerce-infrastructure
  labels:
    app: api-gateway
spec:
  type: LoadBalancer
  ports:
    - port: 5000
      targetPort: 8080
  selector:
    app: api-gateway
# Use the official
FROM postgres:latest

# The official image automatically executes any .sql scripts found in this directory on startup
COPY init-prod-users-db.sql /docker-entrypoint-initdb.d/

EXPOSE 5432